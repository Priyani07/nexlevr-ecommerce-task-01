import {useEffect,useState} from "react";import api from "../services/api";export default function Orders(){const[o,setO]=useState([]);useEffect(()=>{api.get("/orders/mine").then(r=>setO(r.data.orders))},[]);return <main className="page"><span className="eyebrow">ACCOUNT</span><h2>Your Orders</h2>{o.map(x=><article className="order" key={x._id}><b>Order #{x._id.slice(-8)}</b><span>{x.orderStatus}</span><span>${x.totalAmount.toFixed(2)}</span><small>{new Date(x.createdAt).toLocaleString()}</small></article>)}</main>}