import {useState} from "react";import {Link,useNavigate} from "react-router-dom";import {useAuth} from "../context/AuthContext";
export default function Login(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState("");const{login}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();try{const r=await login({email,password});nav(r.user.role==="admin"?"/admin":"/")}catch(x){setError(x.response?.data?.message||"Login failed")}};return <main className="auth"><form onSubmit={submit}><span className="eyebrow">WELCOME BACK</span><h2>Sign in</h2>{error&&<p className="error">{error}</p>}<input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/><button>Sign in</button><p>No account? <Link to="/register">Create one</Link></p></form></main>}