import {useEffect,useState} from "react";import {useParams} from "react-router-dom";import api from "../services/api";import {useCart} from "../context/CartContext";import Loader from "../components/Loader";
export default function ProductDetails(){const{id}=useParams();const[p,setP]=useState(null);const{addToCart}=useCart();useEffect(()=>{api.get(`/products/${id}`).then(r=>setP(r.data.product))},[id]);if(!p)return <Loader/>;return <main className="detail page"><img src={p.image} alt={p.name}/><div><span className="eyebrow">{p.category}</span><h2>{p.name}</h2><p>{p.description}</p><h3>${p.price.toFixed(2)}</h3><p>Rating: {p.rating} / 5 • Stock: {p.stock}</p><button onClick={()=>addToCart(p)}>Add to cart</button></div></main>}